% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_violin_half.R
\docType{data}
\name{GeomViolinHalf}
\alias{GeomViolinHalf}
\alias{geom_violin_half}
\title{Half Violin Plot}
\format{
An object of class \code{GeomViolinHalf} (inherits from \code{GeomViolin}, \code{Geom}, \code{ggproto}, \code{gg}) of length 3.
}
\usage{
GeomViolinHalf

geom_violin_half(
  mapping = NULL,
  data = NULL,
  stat = "ydensity",
  position = "dodge",
  ...,
  side = "left",
  trim = TRUE,
  scale = "area",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2]{aes}()}.}

\item{data}{The data to be displayed in this layer.}

\item{stat}{the statistical transformation to use on the data. Default
\code{"ydensity"}.}

\item{position}{Position adjustment, e.g. \code{\link[ggplot2]{position_nudge}()}.}

\item{...}{other arguments passed to \code{\link[ggplot2]{layer}()}.}

\item{side}{the side on which the density is drawn, \code{"left"} (default) or
\code{"right"}.}

\item{trim}{if \code{TRUE}, trim the tails of the violins to the range of the
data. Default is \code{TRUE}.}

\item{scale}{passed to \code{\link[ggplot2]{geom_violin}()}.}

\item{na.rm}{If \code{FALSE} (default), missing values are removed with a
warning.}

\item{show.legend}{logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\description{
Draws a one-sided (half) violin plot: the density is drawn on a
  single side of each group position, with a flat edge at the center. This is
  the building block of raincloud plots (see \code{\link{ggraincloud}()}),
  where the half violin is the \dQuote{cloud}.
}
\details{
ggplot2 has no built-in half violin, so \code{GeomViolinHalf}
  extends \code{\link[ggplot2]{geom_violin}()} and keeps only one side of the
  density polygon. The idea of the half (\dQuote{flat}) violin comes from the
  raincloud-plot literature (Allen et al., 2019) and prior implementations in
  the \code{ggdist} and \code{gghalves} packages.
}
\examples{
df <- ToothGrowth
df$dose <- as.factor(df$dose)
library(ggplot2)
ggplot(df, aes(dose, len)) +
  geom_violin_half(side = "right", fill = "#00AFBB")

}
\references{
Allen, M., Poggiali, D., Whitaker, K., Marshall, T. R., & Kievit,
  R. A. (2019). Raincloud plots: a multi-platform tool for robust data
  visualization. Wellcome Open Research, 4, 63.
}
\seealso{
\code{\link{ggraincloud}}, \code{\link{ggviolin}}.
}
\keyword{datasets}
