% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_stat_test_label.R
\name{build_symnum_args}
\alias{build_symnum_args}
\title{Build symnum.args from Significance Parameters}
\usage{
build_symnum_args(
  signif.cutoffs = NULL,
  signif.symbols = NULL,
  ns.symbol = "ns",
  use.four.stars = FALSE,
  symnum.args = list()
)
}
\arguments{
\item{signif.cutoffs}{Numeric vector of p-value cutoffs in descending order
(e.g., \code{c(0.10, 0.05, 0.01)} or \code{c(0.10, 0.05, 0.01, 0.001)}).
Values smaller than each cutoff receive the corresponding symbol.}

\item{signif.symbols}{Character vector of symbols matching signif.cutoffs.
If NULL, auto-generated based on length: 3 cutoffs -> c("*", "**", "***"),
4 cutoffs -> c("*", "**", "***", "****").}

\item{ns.symbol}{Character string for non-significant results. Default is "ns".
Use "" (empty string) to show nothing for non-significant results.}

\item{use.four.stars}{Logical. If TRUE and signif.symbols is NULL, include
four stars (****) for the most significant level. Default is FALSE.}

\item{symnum.args}{Existing symnum.args list. If provided and non-empty,
it takes precedence over other parameters (for backward compatibility).}
}
\value{
A list suitable for use as symnum.args parameter.
}
\description{
Internal helper function to build a symnum.args list from
  user-friendly significance parameters. This allows users to specify
  custom significance cutoffs and symbols without using the complex
  symnum.args format directly.
}
\details{
Priority order:
1. If symnum.args is provided (non-empty), use it directly
2. If signif.cutoffs is provided, build symnum.args from it
3. Otherwise, use package defaults
}
\keyword{internal}
