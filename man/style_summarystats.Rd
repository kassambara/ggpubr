% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsummarystats.R
\name{style_summarystats}
\alias{style_summarystats}
\title{Restyle a ggsummarystats Composite}
\usage{
style_summarystats(p, main = NULL, table = NULL)
}
\arguments{
\item{p}{a \code{"ggsummarystats"} object.}

\item{main}{a ggplot component (or a list of components) added to the main
plot (\code{$main.plot}), e.g. \code{theme_bw()} or
\code{list(theme_bw(), labs(title = "My title"))}.}

\item{table}{a ggplot component (or list of components) added to the summary
table (\code{$summary.plot}).}
}
\value{
the updated \code{"ggsummarystats"} object.
}
\description{
Adds ggplot components (themes, labels, scales, ...) to the
  sub-plots of a \code{\link{ggsummarystats}()} object and returns the updated
  object, so the whole composite can be restyled in one call. This is a
  convenience wrapper around editing the \code{$main.plot} and
  \code{$summary.plot} elements directly.
}
\examples{
data("ToothGrowth")
df <- ToothGrowth
df$dose <- as.factor(df$dose)
p <- ggsummarystats(df, x = "dose", y = "len",
  ggfunc = ggboxplot, color = "dose", palette = "npg")

# Restyle the main plot in one call
style_summarystats(p, main = list(theme_bw(), labs(title = "Tooth growth")))
}
\seealso{
\code{\link{ggsummarystats}}.
}
